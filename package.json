{
  "name": "kunalnagarco-ui",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build:all": "yarn workspaces foreach -A -vip --topological-dev run build",
    "lint:all": "yarn workspaces foreach -A -vip --topological-dev run lint"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "20.14.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@4.2.2"
}
