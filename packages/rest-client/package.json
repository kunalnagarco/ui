{
  "name": "@kunalnagarco/ui.rest-client",
  "version": "0.7.0",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "url": "https://github.com/kunalnagarco/ui.git"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@kunalnagarco/ui.jest-config": "^0.10.0",
    "@kunalnagarco/ui.tsconfig": "^0.8.0"
  },
  "scripts": {
    "build": "rimraf dist && tsc",
    "pack:run": "yarn pack",
    "lint": "eslint src/ --ext .ts,.tsx --max-warnings 0",
    "test:watch": "jest --watch"
  }
}
